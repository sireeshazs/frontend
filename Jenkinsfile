@Library('todoapp') _

todoapp (
        COMPONENT             : "frontend"
        PROJECT_NAME          : "TODOAUTOAMATION",
        SLAVE_LABEL           : "MASTER",
        SKIP_NEXUS_UPLOAD     : false,
        APP_TYPE              : "NGINX"
)