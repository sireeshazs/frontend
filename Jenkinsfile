@Library('todo') _

todo (
        COMPONENT             : 'frontend',
        PROJECT_NAME          : "TODOAPP",
        SLAVE_LABEL           : "agent1",
        SKIP_NEXUS_UPLOAD     : false,
        APP_TYPE              : "NGINX"
)