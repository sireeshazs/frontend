@Library('todoapp') _

todoapp (
        COMPONENT             : "frontend",
        PROJECT_NAME          : "TODOAPP"
        SLAVE_LABEL           : "master",
        SKIP_NEXUS_UPLOAD     : false,
        APP_TYPE              : "NGINX"
)